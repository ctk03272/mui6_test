import { styled } from '@mui/material/styles'
import { PageContainer } from '@toolpad/core/PageContainer'

const HollowPageContainer = styled(PageContainer)(() => ({
    minWidth: '100%',
}))

export default HollowPageContainer
